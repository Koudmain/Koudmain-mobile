// eslint-disable-next-line @typescript-eslint/no-require-imports
const tailwindConfig = require('../../../../packages/koudmain-ui/tailwind.config');

export const colors = tailwindConfig.theme.extend.colors;
